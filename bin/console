#!/usr/bin/env ruby
# frozen_string_literal: true

require "bundler/setup"
require "spec_reports_toolbox/test_run"

tr = TestRun.new(3765051133)
# tr.spec_reports.fetch!


# require "pry"
# Pry.start

require "irb"
IRB.start(__FILE__)
